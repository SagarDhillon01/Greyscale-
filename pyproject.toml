[tool.poetry]
name = "opencv-grayscale-project"
version = "0.1.0"
description = "A simple project to convert colored images to grayscale using OpenCV."
authors = ["Your Name <your.email@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
opencv-python-headless = "^4.5.3"
flask = "^3.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"